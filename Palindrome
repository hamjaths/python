class Solution(object):
    def isPalindrome(self, s):
        s1=s.lower()
        a="".join(i for i in s1 if i.isalnum())
        if(a==a[::-1]):
            return True
        else:
            return False
        
        
